import { Section } from '../Section'
import logo from '../../assets/logo.svg'
export const Header = () => {
  return (
    <header>
        <div><img src={logo}/></div>
        <Section />

    </header>
  )
}
